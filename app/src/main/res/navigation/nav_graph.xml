<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.locally.activities.MainActivity"
        android:label="MainActivity" />
    <activity
        android:id="@+id/createOrderActivity"
        android:name="com.example.locally.activities.CreateOrderActivity"
        android:label="activity_create_order"
        tools:layout="@layout/activity_create_order" />
    <activity
        android:id="@+id/editProfileActivity"
        android:name="com.example.locally.activities.EditProfileActivity"
        android:label="activity_edit_profile"
        tools:layout="@layout/activity_edit_profile" />
    <activity
        android:id="@+id/orderDetailsActivity"
        android:name="com.example.locally.activities.OrderDetailsActivity"
        android:label="activity_order_details"
        tools:layout="@layout/activity_order_details" />
</navigation>